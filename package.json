{
  "name": "mood-tracker-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "heroku-postbuild": "npm install --prefix backend && npm install --only=dev --prefix backend && npm run build --prefix backend"
  },
  "dependencies": {
    "npm": "^11.14.1"
  },
  "engines": {
    "node": ">=14.x",
    "npm": ">=6.x"
  }
}
