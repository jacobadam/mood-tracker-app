{
  "name": "mood-tracker-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "heroku-postbuild": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=14.x",
    "npm": ">=6.x"
  }
}
